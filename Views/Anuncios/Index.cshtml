@model IEnumerable<IDonateApp.Models.Anuncio>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OngId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataCriacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataExpiracao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DoacaoId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OngId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataCriacao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataExpiracao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DoacaoId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AnuncioId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AnuncioId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AnuncioId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
